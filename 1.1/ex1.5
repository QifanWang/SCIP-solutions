Using normal-order evaluaion,

	(test 0 (p))
	(if (= 0 0) 0 (p))
	0

the expression evaluates to 0.

But using applicative-order evaluation,

	(test 0 (p))
	(test 0 (p))
	...

the expression will infinitely expand.

